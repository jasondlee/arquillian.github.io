version: "3"

services:
  my-jekyll-container:
    image: my-ruby-env:latest
    container_name: local_jekyll
    environment:
      GEM_HOME: .gems
    restart: always
    tty: true
    volumes:
      - .:/app
    ports:
      - "4000:4000"